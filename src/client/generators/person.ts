const FirstNames = {
  male: [
    "Xander"
  ],
  female: [
    "Rachel"
  ]
}

const LastNames = [
  "Tanner",
  "Harrison"
]

const Genders = [
  "male",
  "female"
]

const PedModels = {
  male: [],
  female: []
}

// export function GeneratePerson() : object {
//   const gender = Math.floor(Math.random() * Genders.length);
//   const firstname = Math.floor(Math.random() * FirstNames[gender]);
//   const lastname = Math.floor(Math.random() * LastNames)
//   const model = Math.floor(Math.random() * PedModels[gender].length);
//   return { gender, firstname, lastname, model };
// }